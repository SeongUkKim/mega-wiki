mega-wiki:
  storage: memory
  slack:
    enabled: true
    mode: ${SLACK_MODE:socket}
    bot-token: ${SLACK_BOT_TOKEN:}
    app-token: ${SLACK_APP_TOKEN:}
    signing-secret: ${SLACK_SIGNING_SECRET:}
    bot-user-id: ${SLACK_BOT_USER_ID:}
  gemini:
    enabled: false
  snowflake:
    enabled: true
    api-url: ${SNOWFLAKE_API_URL:https://mm0292ev17.execute-api.ap-northeast-2.amazonaws.com}
